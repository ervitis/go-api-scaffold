package controllers

import (
  "<%- repoName %>/<%- userName %>/<%- projectName %>/controllers/_routing"
  "net/http"
)

func Routes() _routing.Handlers {
  return _routing.Handlers{
    // here it goes the routes and handlers
  }
}