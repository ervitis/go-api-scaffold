package main

import (
  "<%- repoName %>/<%- repoUser %>/<%- projectName %>/controllers"
  "<%- repoName %>/<%- repoUser %>/<%- projectName %>/controllers/_routing"
  "<%- repoName %>/<%- repoUser %>/<%- projectName %>/server"
  "net/http"
)

func main() {
  srv := &server.Server{Addr: "http://localhost", Port: "8080"}
  r, err := _routing.MainController(controllers.Routes())
  if err != nil {
    panic(err)
  }

  if err = http.ListenAndServe(srv.PortAddress(), r); err != nil {
    panic(err)
  }
}